# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running ShioriCode (for example via API key or ChatGPT auth supported by Codex).
- ShioriCode starts the server via `codex app-server` per session.
