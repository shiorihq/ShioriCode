/**
 * One body scale for the chat thread: assistant markdown, work log, timestamps,
 * completion notes, user bubbles, and related tool surfaces. Use this (not ad-hoc
 * text-xs / arbitrary px) so lines don’t look like different font sizes.
 */
export const CHAT_THREAD_BODY_CLASS = "text-sm font-normal leading-relaxed" as const;
